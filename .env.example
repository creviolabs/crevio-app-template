CREVIO_API_KEY=""
CREVIO_API_BASE_URL=""

# Where "Sign in with Crevio" sends visitors (dispatch worker authorize flow)
CREVIO_SIGN_IN_URL=""

# Public keys for verifying the signed user token (defaults to production).
CREVIO_JWKS_URL=""

# Dev / preview identity — OPTIONAL. In dev the gated experience already renders
# for a synthetic admin preview with no config. Set these only to impersonate a
# REAL member so access.check runs against the live API (gated vs. ungated).
# NEVER set in production.
CREVIO_DEV_USER=""
CREVIO_DEV_ACCOUNT=""
